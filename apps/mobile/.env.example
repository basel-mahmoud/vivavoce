# Backend API origin (the Next.js app). EXPO_PUBLIC_ vars are embedded in the build.
EXPO_PUBLIC_API_URL="http://localhost:3000"

# Clerk publishable key (safe to embed; secret key lives only on the backend).
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_xxx"
